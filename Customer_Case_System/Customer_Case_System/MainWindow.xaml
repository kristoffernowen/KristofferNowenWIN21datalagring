<Window x:Class="Customer_Case_System.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Customer_Case_System"
        xmlns:viewModel="clr-namespace:Customer_Case_System.Models.ViewModels"

        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

<Window.DataContext>
    <viewModel:MainWindowViewModel></viewModel:MainWindowViewModel>
</Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" >
            <Button Content="Register Customer" Margin="10" Command="{Binding RegisterCustomerViewCommand}"></Button>
            <Button Content="Register Case" Margin="10" Command="{Binding RegisterCaseViewCommand}"></Button>
            <Button Content="Display Customers" Margin="10" Command="{Binding DisplayCustomersViewCommand}"></Button>
            <Button Content="Display cases" Margin="10" Command="{Binding DisplayCasesViewCommand}"></Button>
        </StackPanel>

        <ContentControl Grid.Row="1" Content="{Binding CurrentView}" ></ContentControl>
    </Grid>
</Window>
